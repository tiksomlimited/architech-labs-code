{
    "require-dev": {
        "phpunit/phpunit": "^10.5",
        "phpstan/phpstan": "^2.0"
    },
    "scripts": {
        "test": "phpunit",
        "analyse": "phpstan analyse"
    },
    "autoload": {
        "psr-4": {
            "App\\Basket\\": "src/"
        }
    }    
}
